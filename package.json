{
    "name": "management",
    "version": "1.0.0",
    "scripts": {
        "cleint": "cd client && yarn start",
        "server": "nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"yarn start\" \"yarn client\""
    },
    "dependencies": {
        "body-parser": "^1.20.1",
        "express": "^4.18.2",
        "nodemon": "^2.0.20"
    },
    "devDependencies": {
        "concurrently": "^4.0.1"
    }
}
